services:
  cache:
    image: redis:latest
    ports:
      - 6379:6379
    volumes:
      - redis-data:/data
  
  
  app:
    build: .
    ports:
      - 5000:5000
    environment:
      - REDIS_HOST=cache
    depends_on:
      - cache
    
volumes:
  redis-data: